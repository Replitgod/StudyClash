// The Twitter/X card is the same artwork as the Open Graph card. Kept as a
// re-export rather than a second copy so the two can never drift into
// showing different brands.
export {
  default,
  alt,
  size,
  contentType,
} from "./opengraph-image";
